import { FirebaseAuthInterceptor } from './firebase-auth.interceptor';

describe('FirebaseAuthInterceptor', () => {
  it('should be defined', () => {
    expect(new FirebaseAuthInterceptor()).toBeDefined();
  });
});
